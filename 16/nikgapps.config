# NikGapps configuration file

# If you are not sure about the config, just skip making changes to it or comment it by adding # before it


AndroidVersion=16
Version=37

LogDirectory=default
InstallPartition=default
Mode=install
WipeDalvikCache=1
WipeRuntimePermissions=0
ExecuteBackupRestore=1
UseZipConfig=0
OverwriteWithZipConfig=0
GmsOptimization=0
GenerateLogs=1

# Core packages (essential Google packages)
Core=1
>>GooglePlayStore=1
>>GoogleServicesFramework=1
>>GoogleContactsSyncAdapter=1
>>GoogleCalendarSyncAdapter=1
>>GmsCore=1

# Your requested apps (no extra, just the core Google experience)
GooglePhotos=1        # Replaces AOSP Gallery
Gmail=1               # Replaces AOSP Email
Gboard=1              # Google Keyboard
GoogleMessages=1      # Replaces AOSP Messages
GoogleWallpaper=1     # Pixel Wallpaper
PixelThemes=1         # Pixel Themes
GoogleChrome=1        # Replaces AOSP Browser
GoogleCarrierServices=1
GoogleContacts=1      # Replaces AOSP Contacts
MarkupGoogle=1        # Google Markup
GoogleSounds=1        # Google Sounds (Ringtones, notifications)

# No Pixel-specific apps enabled (no extra apps installed)
PixelSpecifics=0

# Skip all Go apps (CoreGo is set to 0 to avoid Go versions)
CoreGo=0
